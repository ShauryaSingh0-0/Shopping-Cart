The code firstly includes <stdio.h> which is an important library to use format specifiers for different variables
Then includes <string.h>, For using the line 10 in the source code this is an important library
Then this line pushes us into the main builders code
Then this line takes variable "item" with 50 characters and format specifier char
Then this line takes variable "price" with 50 characters and format specifier float
Then this line takes variable "quantity" with 50 characters and format specifier int
Then this line takes variable "total" with 50 characters and format specifier float
Then this line takes variable "currency" with 50 characters and format specifier char
this line prints "Enter item name:"
this line gets input from user and stores it into variable "item" and also keeps the no. of characters according to assigned value i.e 50
this line helps us to overcome input buffering (Uses <string.h>)

this line prints "Enter price:"
this line gets input from user and stores it into variable "price"

this line prints "Enter quantity:"
this line gets input from user and stores it into variable "quantity"

this line performs an arithmetic operation of multiplication of "quantity" and "price" and stores it in "total" variable

this line prints "Enter item name:"
this line helps old c version to sumerize
END
*PS This is my one of the first projects for C 
